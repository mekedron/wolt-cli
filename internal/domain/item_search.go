package domain

const (
	// GlobalItemSearchDefaultLimit is the default number of globally ranked
	// item matches requested by user-facing clients.
	GlobalItemSearchDefaultLimit = 20
	// GlobalItemSearchMaxLimit is the largest item-search limit accepted by
	// the current Wolt search endpoint.
	GlobalItemSearchMaxLimit = 200
)
